<!-- / .navbar-fixed-left.sidebar
	
/ 	.toggle-button
/ 		.menu-bar.menu-bar-top
/ 		.menu-bar.menu-bar-middle
/ 		.menu-bar.menu-bar-bottom

	
	
/ %header
/   %span.toggle-button
/     .menu-bar.menu-bar-top
/     .menu-bar.menu-bar-middle
/     .menu-bar.menu-bar-bottom
/   .menu-wrap
/     .menu-sidebar
/       %ul.menu
/       -courses
/       - if @courses.empty?
/         %h3 You are currently not enrolled in any courses
/       - else
/         -@courses.each do |course|
/         -find_learn(course.learn_id)
/           -@learn.each do |learn| 
/             %h4=link_to learn.title, "/learn/#{learn.id}", class: "darkGrey"
/             -learn.lectures.each do |lecture|
/               %li=link_to lecture.title, "/learn/#{learn.id}/lecture/#{lecture.id}"


%span.toggle-button
  .menu-bar.menu-bar-top
  .menu-bar.menu-bar-middle
  .menu-bar.menu-bar-bottom
.menu-wrap
  .menu-sidebar
    %ul.menu
    - courses
    - if @courses.empty?
      %h3 You are currently not enrolled in any courses
    - else
      - @courses.each do |course|
        .row
          - find_learn(course.learn_id)
          - @learn.each do |learn|
            .dropdown
              %h4.dropdown-toggle{"data-toggle" => "dropdown"}
                = learn.title
                %span.caret
              %ul.dropdown-menu.relative
                - learn.lectures.each do |lecture|
                  %li
                    = link_to lecture.title, "/learn/#{learn.id}/lecture/#{lecture.id}"



/ <span class="toggle-button">
/   <div class="menu-bar menu-bar-top"></div>
/   <div class="menu-bar menu-bar-middle"></div>
/   <div class="menu-bar menu-bar-bottom"></div>
/ </span>
/ <div class="menu-wrap">
/   <div class="menu-sidebar">
/     <ul class="menu"></ul>
/       <% courses %>
/         <% if @courses.empty? %>
/           <h3>You are currently not enrolled in any courses</h3>
/         <% else %>
/           <% @courses.each do |course| %>
/             <div class='row'>
/               <% find_learn(course.learn_id) %>
/               <% @learn.each do |learn|  %>
/               <div class="dropdown">
/                 <h4 class="dropdown-toggle" data-toggle="dropdown"><%= learn.title %><span class="caret"></span></h4>
/                 <ul class="dropdown-menu relative">
/                 <% learn.lectures.each do |lecture| %>
/                   <li>
/                     <%= link_to lecture.title, "/learn/#{learn.id}/lecture/#{lecture.id}" %>
/                   </li>
/                 <% end %>
/                 </ul>
/               </div>
/             </div>
/               <% end %>
/           <% end %>
/         <% end %>
/   </div>
/ </div>
