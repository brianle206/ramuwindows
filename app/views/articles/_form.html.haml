= simple_form_for @article do |f|
	=	f.collection_select :category_id, Category.all, :id, :name, {promt: "Choose a Category"}
	=	f.input :title
	= 	f.input :content,:input_html => {:rows => 10, :placeholder => "Enter some text.", :class => "span6", "data-provide" => "markdown" }
	= 	f.submit